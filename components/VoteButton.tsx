'use client';import {useState} from 'react';export default function VoteButton({pokemonId,initialVotes}:{pokemonId:number;initialVotes:number}){const[v,setV]=useState(initialVotes);const[busy,setBusy]=useState(false);async function vote(){if(busy)return;setBusy(true);const r=await fetch('/api/vote',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({pokemonId})});const d=await r.json();if(r.ok)setV(d.votes);alert(d.error||'투표했습니다!');setBusy(false)}return <button className="btn" style={{marginTop:18}} onClick={vote} disabled={busy}>♥ 인기투표 · {v}</button>}
