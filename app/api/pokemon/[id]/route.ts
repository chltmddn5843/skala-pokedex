import {getPrisma} from '@/lib/prisma';export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const id=Number((await params).id);if(!Number.isInteger(id))return Response.json({error:'invalid id'},{status:400});const p=await getPrisma().pokemon.findUnique({where:{id},include:{owners:true,_count:{select:{votes:true}}}});return p?Response.json(p):Response.json({error:'not found'},{status:404})}
