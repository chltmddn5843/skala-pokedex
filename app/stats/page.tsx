import {getPrisma} from '@/lib/prisma';export const dynamic='force-dynamic';export default async function Stats(){const db=getPrisma();const top=await db.pokemon.findMany({orderBy:{votes:{_count:'desc'}},take:10,include:{_count:{select:{votes:true,owners:true}}}});const campus=await db.ownership.groupBy({by:['campus'],_count:{_all:true},orderBy:{_count:{campus:'desc'}}});return <section><div className="eyebrow">SKALA 4기 · COMMUNITY STATS</div><h1>분포 & 인기 랭킹</h1><div className="detail"><div className="panel"><h2>인기투표 TOP 10</h2><div className="rank">{top.map((p,i)=><div className="rankrow" key={p.id}><b>#{i+1}</b><span>{p.nameKo} <span className="muted">· 보유 {p._count.owners}</span></span><b>{p._count.votes}</b></div>)}</div></div><div className="panel"><h2>캠퍼스 분포</h2><div className="rank">{campus.map((c)=><div className="rankrow" key={c.campus}><span>{c.campus}</span><div className="bar"><i style={{width:`${Math.min(100,c._count._all)}%`}}/></div><b>{c._count._all}</b></div>)}</div></div></div></section>}
